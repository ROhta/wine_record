# ── Upstash Vector（必須）──────────────────────────────────────
# 埋め込みモデル BAAI/bge-m3（dense・1024次元・COSINE）でインデックス作成済みのもの。
# Vercel の Upstash Vector 統合（Free プラン）から作成できる。詳細は
# specs/001-record-wine/research.md R1。
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# ── 画像ストレージ（US3 用・任意）──────────────────────────────
# ラベル画像の永続化（US3）は確認ウィジェット（widget）の claude.ai リモート HTTP 対応待ちで
# 未着手のため、US1+US2 の稼働には不要。未設定でもサーバーは起動する。
# 未設定なら record_wine は imageUrl を受理しない（fail-closed）。
# 方針: R3 で Cloudflare R2 → Vercel Blob（private）へ変更済み。US3 実装時に変数名を見直す。
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# 保存後に参照する公開ベース URL（例: https://images.example.com）
R2_PUBLIC_BASE_URL=

# ── MCP コネクタ OAuth 認証（Auth0・US 002）──────────────────────
# claude.ai から認証付きで接続するための OAuth（リソースサーバー）設定。
# 両方設定で認証 ON / 両方未設定で OFF（ローカル開発・テスト）。本番では必ず設定する。
# 片方のみはエラー。詳細は specs/002-mcp-oauth-auth/quickstart.md。
# AUTH0_ISSUER_BASE_URL: Auth0 テナント発行者 URL（例 https://<tenant>.<region>.auth0.com）
# AUTH0_AUDIENCE: Auth0 API Identifier（= 正規 MCP URL。例 https://wine-record-rohta.vercel.app/mcp）
AUTH0_ISSUER_BASE_URL=
AUTH0_AUDIENCE=

# ── サーバー ───────────────────────────────────────────────────
# MCP サーバー（Streamable HTTP）が listen するポート（既定 3000）
PORT=3000
